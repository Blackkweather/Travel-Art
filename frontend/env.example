# Environment Configuration for Travel Art Frontend

# Development/Production mode
VITE_MODE=development

# CDN Provider Configuration
# Options: local, jsdelivr, cloudflare, aws, vercel, cloudinary
# For production, use: cloudinary
VITE_CDN_PROVIDER=cloudinary

# API Configuration
VITE_API_URL=http://localhost:4000/api

# CDN Configuration (only used when VITE_CDN_PROVIDER is not 'local')

# JSDelivr CDN (GitHub-based)
VITE_JSDELIVR_USERNAME=yourusername
VITE_JSDELIVR_REPO=travel-art-assets
VITE_JSDELIVR_BRANCH=main

# Cloudflare R2
VITE_CLOUDFLARE_BUCKET=your-bucket
VITE_CLOUDFLARE_ACCOUNT=your-account

# AWS S3
VITE_AWS_BUCKET=your-bucket
VITE_AWS_REGION=us-east-1

# Vercel Blob
VITE_VERCEL_BLOB_STORE=your-blob-store

# Cloudinary
VITE_CLOUDINARY_CLOUD_NAME=desowqsmy
VITE_CLOUDINARY_VERSION=v1761401364

# Use Cloudinary in production ("true" to prefer CDN when building prod)
# Set to "false" to always use local/public assets unless overridden below
VITE_USE_CLOUDINARY=true

# Logo URL overrides (recommended for production to avoid server rewrites)
# Point these to your CDN or storage URLs to force exact images in live
# Example: https://cdn.example.com/assets/brand/logo.png
VITE_LOGO_FINAL_URL=
VITE_LOGO_TRANSPARENT_URL=